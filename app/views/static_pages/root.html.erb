
    <% if current_user %>

      <script id="bootstrap-current-user">

        window.currentUser = {
          "id": <%= current_user.id %>,
          "username": "<%= current_user.username %>",
          "email": "<%= current_user.email %>"
        };

      </script>
    <% end %>

<div id="root"></div>